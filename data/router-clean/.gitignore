# Per-domain JSONL files are regenerated by scripts/rebuild_router_dataset.py.
# They contain prompts from various HF datasets — re-fetch is faster than
# committing them and keeps the repo size manageable.
*.jsonl
